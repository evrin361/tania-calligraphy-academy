import type { Config } from "tailwindcss";

// Tailwind CSS v4 performs automatic content detection and no longer
// requires an explicit `content` array. This file is kept as an explicit
// extension point for future design tokens (see `src/theme/`), which are
// intentionally not implemented yet as part of this task.
const config: Config = {};

export default config;
