# Architecture Decision Records (ADR)

This directory holds records of significant architectural decisions made
throughout the project's lifecycle.

Each ADR captures the context behind a decision, the decision itself, and
its consequences, so that future contributors understand not just _what_
was decided, but _why_.

Use `adr-template.md` as the starting point when drafting a new ADR.
