# Missions

This directory holds specifications for individual tasks or missions
assigned to contributors — human or AI — during the project's lifecycle.

Each mission document defines an objective, scope, deliverables, and
acceptance criteria for a discrete unit of work.

Use `mission-template.md` as the starting point when drafting a new
mission specification.
